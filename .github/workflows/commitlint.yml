name: Conventional commits lint

on:
  pull_request:

jobs:
  commitlint:
    runs-on: ubuntu-latest
    steps:
      - name: Commitlint (Conventional Commits)
        uses: wagoid/commitlint-github-action@v6
        with:
          configFile: "" # uses default conventional commits rules